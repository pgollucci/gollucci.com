# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

queue_rules:
  - name: default
    conditions:
      - "#approved-reviews-by>=1"
      - -label~=(do-not-merge)
      - check-success=Validate PR title
      - check-success=Build
    commit_message_template: |-
      {{ title }} (#{{ number }})

      {{ body }}
    merge_method: squash  # Moved from pull_request_rules to queue_rules

pull_request_rules:
  - name: Automatic merge on approval and successful build
    actions:
      delete_head_branch: {}
      queue:
        name: default  # Referencing the queue by name only
    conditions:
      - "#approved-reviews-by>=1"
      - -label~=(do-not-merge)
      - check-success=Validate PR title
      - check-success=Build

